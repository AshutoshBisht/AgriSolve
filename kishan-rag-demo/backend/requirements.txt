fastapi
uvicorn
python-dotenv
pydantic
pinecone
langchain-text-splitters
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
sentence-transformers  # all-MiniLM-L6-v2 — 384-dim, ~90MB RAM
# BAAI/bge-large-en-v1.5 and cross-encoder removed — were ~750MB RAM combined
# pypdf
pymupdf
google-generativeai
python-multipart
groq  # Whisper transcription via Groq API (free tier, no local model needed)
beautifulsoup4  # HTML parsing for URL content scraping
httpx  # Async HTTP client for web scraping
agno   # Lightweight agent framework for Gemini autonomous tool-calling agents
# torch / transformers / librosa / soundfile / resampy removed — not needed with Groq
